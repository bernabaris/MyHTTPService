version: '3'
services:
  backend:
    environment:
      - PORT=5050
    deploy:
      replicas: 2
    networks:
      - loadbalancing

  nginx:
    container_name: nginx
    ports:
      - "80:80"
    networks:
      - loadbalancing
    depends_on:
      - backend

networks:
  loadbalancing: